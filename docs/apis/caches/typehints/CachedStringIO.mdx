---
id: CachedStringIO
title: CachedStringIO
sidebar_label: CachedStringIO
slug: /apis/caches/typehints/CachedStringIO
description: The data of one cached StringIO data.
---

import APITopBar, {IconObjType} from "@site/src/components/APITopBar";

<APITopBar type="type" source="caches/typehints.py#L93" />

```python
CachedStringIO(
    type: "str",
    data: IO[str]
)
```

`CachedStringIO` is a `TypedDict`.

`CachedStringIO` is the data of one cached `StringIO` data.

## Keywords

| Keywork |   Type    | <center>Description</center>                                                                             |
| :-----: | :-------: | :------------------------------------------------------------------------------------------------------- |
| `type`  |  `"str"`  | The type os this cached data.                                                                            |
| `data`  | `IO[str]` | The text data to be cached, the type of the file can be determined by the file name and the `mime_type`. |

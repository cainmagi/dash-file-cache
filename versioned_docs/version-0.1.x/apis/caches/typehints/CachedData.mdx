---
id: CachedData
title: CachedData
sidebar_label: CachedData
slug: /apis/caches/typehints/CachedData
description: The typehint of the cached data.
---

import APITopBar, {IconObjType} from "@site/src/components/APITopBar";

<APITopBar type="type" source="caches.typehints.CachedData" />

```python
CachedData = CachedPath | CachedStringIO | CachedBytesIO
```

`CachedData` is a `Union` of [`CachedPath`](./CachedPath.mdx),
[`CachedStringIO`](./CachedStringIO.mdx), and [`CachedBytesIO`](./CachedBytesIO.mdx).

`CachedData` is the typehint of the cached data.
